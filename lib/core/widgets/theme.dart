import 'package:flutter/material.dart';

class CoffeeThemeColors {
  static const primary = Color(0xFF6F4E37);
  static const secondary = Color(0xFFC69C6D);
  static const background = Color(0xFFF7F1EA);
  static const textPrimary = Color(0xFF4B2E05);
}

class CoffeeDarkThemeColors {
  static const primary = Color(0xFF8B5E3C);      // Lebih terang dari coklat gelap agar tetap kontras
  static const secondary = Color(0xFFD2B48C);    // Warna kopi susu sebagai aksen
  static const background = Color(0xFF1E1E1E);   // Latar belakang gelap
  static const textPrimary = Color(0xFFF5F5F5);  // Warna teks terang agar terbaca di background gelap
}
